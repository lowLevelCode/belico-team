{
   "type": "mongodb",
   "host": "localhost",   
   "database": "test",
   "synchronize": true,
   "logging": true,
   "entities": [
      "src/models/**/*.ts"
   ],
   "extra": { "insecureAuth": false } ,
   "cli": {
      "entitiesDir": "src/models" 
   }
}